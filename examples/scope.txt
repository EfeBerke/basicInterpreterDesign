(* scope.txt - static scoping using defining environment rather than calling *)

let x = 10;

let make_function = fun() ->
    let x = 20;

    fun(y) ->
        x + y
    end
end;

let f = make_function();
(* calling from global but x stays 20 because of the static scoping behaviour *)
print(f(5));